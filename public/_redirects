# Netlify/Vercel SPA redirect rules
# This ensures all routes are handled by the React app

# API routes should go to backend
/api/* https://youngeagles-api.up.railway.app/api/:splat 200

# All other routes should serve the React app
/* /index.html 200
